[package]
name = "post"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
anyhow = "1.0"
argon2 = { version = "0.5", features = ["std"] }
async-nats = { version = "0.43", optional = true }
aws-config = { version = "1.5", optional = true }
aws-sdk-s3 = { version = "1.66", optional = true }
axum = { version = "0.8", features = ["macros", "ws"], optional = true }
bytes = "1.11"
console_error_panic_hook = { version = "0.1", optional = true }
elasticsearch = { version = "8.19.0-alpha.1", optional = true }
getrandom = { version = "0.2", features = ["js"] }
leptos = { version = "0.8.0" }
leptos_axum = { version = "0.8.0", optional = true }
leptos_meta = { version = "0.8.0" }
leptos_router = { version = "0.8.0" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sqlx = { version = "0.9.0", features = ["postgres", "runtime-tokio", "macros", "time", "uuid"], optional = true }
thiserror = "2.0"
time = { version = "0.3", features = ["serde"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros"], optional = true }
uuid = { version = "1.12", features = ["serde"] }
wasm-bindgen = { version = "=0.2.105", optional = true }

[dev-dependencies]
pretty_assertions = "1.4"

[features]
default = ["ssr"]
hydrate = [
    "leptos/hydrate",
    "dep:console_error_panic_hook",
    "dep:wasm-bindgen",
]
ssr = [
    "dep:axum",
    "dep:async-nats",
    "dep:aws-config",
    "dep:aws-sdk-s3",
    "dep:tokio",
    "dep:elasticsearch",
    "dep:leptos_axum",
    "dep:sqlx",
    "leptos/ssr",
    "leptos_meta/ssr",
    "leptos_router/ssr",
]

[profile.wasm-release]
inherits = "release"
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"

[package.metadata.leptos]
output-name = "post"
site-root = "target/site"
site-pkg-dir = "pkg"
style-file = "style/main.css"
assets-dir = "public"
site-addr = "127.0.0.1:3000"
reload-port = 3001
browserquery = "defaults"
env = "DEV"
bin-features = ["ssr"]
bin-default-features = false
lib-features = ["hydrate"]
lib-default-features = false
lib-profile-release = "wasm-release"
