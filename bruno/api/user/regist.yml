info:
  name: 用户注册
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/user/regist"
  headers:
    - name: content-type
      value: application/json
  body:
    type: json
    data: |-
      {
        "username": "test_user",
        "password": "111111",
        "display_name": "测试用户"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
