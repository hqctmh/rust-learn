[package]
name = "toasty-demo"
version = "0.1.0"
edition = "2024"

[dependencies]

tokio = { version = "1", features = ["full"] }
sqlx = { version = "0.9.0", features = ["sqlite", "runtime-tokio", "macros"] }
